const validateInputs = (req, res, next) => {
    const { username, password, idNum, accNum, amount, currency, swiftCode } = req.body;

    //regex patterns for input whitelisting (need to still do some research on these to implement)
    const usernameRegex = ;
    const passwordRegex = ;
    const idNumRegex = ;
    const accNumRegex = ;
    const amountRegex = ;
    const currencyRegex = ;
    const swiftCodeRegex = ;

    

}